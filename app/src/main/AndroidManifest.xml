<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.item">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.me.click.LikeActivity"></activity>
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.notifications.ClassifyActivity" />
        <activity android:name=".ui.home.adapter.ShopActivity" />
        <activity android:name=".ui.home.bigvideo.BigImageActivity" />
        <activity android:name=".ui.home.activity.SubHomeDirectActivity" />
        <activity android:name=".ui.home.activity.HomeNewActivity" />
        <activity android:name=".ui.home.activity.HomeDirectActivity" />
        <activity android:name=".ui.home.DetailActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>